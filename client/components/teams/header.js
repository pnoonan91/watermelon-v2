import React from 'react'
import {Header} from 'semantic-ui-react'

const TeamsHeader = (props) => (
    <Header color="pink" size="huge" textAlign="center">Teams</Header>
)

export default TeamsHeader
